export default class UserInterestModel {
  id: number;
  title: number;
  wage: number;

  constructor(id: number, title: number, wage: number) {
    this.id = id;
    this.title = title;
    this.wage = wage;
  }
}
